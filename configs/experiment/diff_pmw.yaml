# @package _global_
defaults:
  - override /sources: pmw_37_89
  - override /lightning_module: flow_matching
  - override /lr_scheduler: cosine_warmup_100epochs
  - override /metrics: none
  - override /model: motif_10b_d256

lightning_module:
  patch_size: 8
  use_det_model_from_run: docl3i8w

wandb:
  name: diff_pmw
  group: diff_pmw

dataset:
  train:
    dt_max: 6
    min_available_sources: 2
    source_types_max_avail:
      pmw: 3
  
trainer:
  strategy: ddp
